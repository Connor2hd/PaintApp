@model IEnumerable<PaintAppModels.PaintGroup>

@{
    ViewData["Title"] = "Index";
}

<h1>Index</h1>

<p>
    <a asp-action="Create">Create New</a>
</p>
<table class="table">
    <thead>
        <tr>
            <th>
                @Html.DisplayNameFor(model => model.ManufacturerId)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.PaintGroupName)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.PaintGroupDescription)
            </th>
            <th></th>
        </tr>
    </thead>
    <tbody>
@foreach (var item in Model) {
        <tr>
            <td>
                @Html.DisplayFor(modelItem => item.ManufacturerId)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.PaintGroupName)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.PaintGroupDescription)
            </td>
            <td>
                <a asp-action="Edit" asp-route-id="@item.PaintGroupId">Edit</a> |
                <a asp-action="Details" asp-route-id="@item.PaintGroupId">Details</a> |
                <a asp-action="Delete" asp-route-id="@item.PaintGroupId">Delete</a>
            </td>
        </tr>
}
    </tbody>
</table>
